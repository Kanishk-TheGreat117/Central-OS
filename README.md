# Central OS Project

A custom GTK4 shell with animated transitions, glass-style dock, and file explorer.

## 🔧 Requirements
- GTK4
- GCC
- Linux or Windows with MSYS2

## 🧪 Compile and Run
```bash
gcc central_os.c -o central_os.exe `pkg-config --cflags --libs gtk4`
./central_os.exe
